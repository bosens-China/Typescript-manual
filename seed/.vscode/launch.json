{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "typescript程序调试",
      "type": "node",
      "request": "launch",
      "program": "${workspaceRoot}/node_modules/ts-node/dist/bin.js",
      "args": [
          "${relativeFile}"
      ],
      "cwd": "${workspaceRoot}",
      "protocol": "inspector"
  }
  ]
}